apiVersion: v2
name: mlflow-core
description: MLflow Core dependency chart
type: application
version: 0.0.1

dependencies:
  - name: postgres
    version: "0.1.0"
    repository: "file://charts/postgres"
    condition: postgres.enabled

  # - name: garage
  #   version: "0.9.2" # must match garage Chart.yaml version
  #   repository: "file://charts/garage"
  #   condition: garage.enabled
